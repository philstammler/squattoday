{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -r build && tsc && nodemon",
    "build": "rm -r build && tsc"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.16",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "express": "^4.17.2",
    "nodemon": "^2.0.15",
    "ts-node": "^10.5.0"
  }
}
